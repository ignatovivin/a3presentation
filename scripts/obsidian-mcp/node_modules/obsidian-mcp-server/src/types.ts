import { z } from "zod";

export type tool<Args = Record<string, unknown>> = {
  name: string;
  description: string;
  schema: Record<string, z.ZodTypeAny>;
  handler: (args: Args, extra: unknown) => {
    content: Array<{
      type: "text";
      text: string;
    }>;
  };
};
